# Dépendance à ajouter

Ce zip ne contient PAS de package.json modifié (pour éviter d'écraser tes deps si elles ont bougé).
Avant de builder, lance dans le repo :

    npm install framer-motion

C'est la seule nouvelle dépendance. Rien d'autre n'a changé côté deps.
